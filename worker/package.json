{
  "name": "kaist-run-auth-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "sync-members": "curl -X POST -H \"x-admin-secret: $ADMIN_SYNC_SECRET\" https://kaist.run/api/admin/sync-members"
  },
  "dependencies": {
    "hono": "^4",
    "jose": "^5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "typescript": "^5",
    "wrangler": "^4"
  }
}
