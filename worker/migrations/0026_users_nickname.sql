-- 사이트 전반에서 쓰는 닉네임. 실명(name)과 별개로 회원이 마이페이지에서 언제든 고칠 수
-- 있고, 회원 간 중복도 허용합니다(유니크 인덱스 없음 — 의도된 것).
--
-- NULL과 빈 문자열('')을 구분합니다:
--   NULL — 아직 정해진 적 없음. 봇 가입 API가 nickname을 안 보내면 Discord 표시 이름으로
--          채우므로, 이 마이그레이션 이전에 등록된 기존 회원만 여기 남습니다.
--   ''   — 회원이 "닉네임 없음"을 명시적으로 고른 상태. 기본값으로 되돌리지 않습니다.
-- 화면에는 둘 다 "(닉네임 없음)"으로 똑같이 보이지만, 가입 시 기본값을 채울지 말지를
-- 가르는 기준이라 저장 단계에서는 구분해서 둡니다.
ALTER TABLE users ADD COLUMN nickname TEXT;
